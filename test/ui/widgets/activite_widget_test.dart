void main() {
  /*testWidgets(
    'activite widget ...',
    (tester) async {
      await tester.pumpWidget(
        MaterialApp(
          home: Expanded(
            child: ActiviteWidget(
              typeActivite: fakeActivite,
            ),
          ),
        ),
      );

      expect(find.byType(CircleAvatar), findsOneWidget);
      expect(
          find.text("Vous avez fais : ${fakeActivite.type}"), findsOneWidget);
    },
  );*/
}
